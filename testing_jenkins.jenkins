#! groovy
properties ([disableConcurrentBuilds()])
pipeline {
    agent any
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Test') {
            steps {
                python -m venv venv
            }
        }

    }
}