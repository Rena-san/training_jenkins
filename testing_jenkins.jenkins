#! groovy
properties ([disableConcurrentBuilds()])
pipeline {
    agent any
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Test') {
            steps {
                sh python -m venv venv
            }
        }

    }
}